# p4-003 — Create `inthub-data-inventory` lib

**Status:** `done`

Scaffold `packages/inthub-data-inventory` as an Nx TypeScript library. Export a `database: Database` object with realistic fake data for the inventory sub-app (Phase 05).

## Data Content

**2–3 Orgs** with distinct profiles:
- **Acme Corp** — large enterprise, many providers, complex integrations
- **Titan Security** — mid-size, focused set of providers
- **Vertex Labs** — small, minimal setup (onboarding)

**Providers (shared catalog):**
- Cloud: AWS, GCP, Azure
- Vulnerability: Qualys, Tenable
- SIEM: DataDog, QRadar, Splunk
- Data: Snowflake

**Per-org data:**
- ProviderAccounts with realistic externalIds
- CredentialTypes per provider
- Credentials with varied statuses (active, expiring, expired)
- Configs (Qualys domain, DataDog site, etc.)
- IntegrationRuntimes (Astronomer, N8N, EKS)
- IntegrationTypes (versioned pipeline templates)
- Integrations (Qualys→Snowflake, DataDog→Snowflake, etc.)
- HealthChecks, bindings, and sample runs
- ScheduledTasks for integrations and health checks

All IDs should be referentially consistent (no dangling foreign keys).

## Definition of Done

- `packages/inthub-data-inventory/` exports `database: Database`
- Data covers all 15 entity types
- At least 2 orgs with meaningful variety
- All references are valid (providerAccountId → existing ProviderAccount, etc.)
- `pnpm nx build inthub-data-inventory` succeeds
- Depends on `@rp-vibe-ideation/inthub-entities`
