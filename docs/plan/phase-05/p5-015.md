# p5-015 — Rename inthub-inventory → inthub-v1, inthub-data-inventory → inthub-data, remove sidebar header

**Status:** `done`

Three coordinated changes:

1. **UI** — remove the static `IntHubHeader` from the sidebar; rename the app to "IntHub v1" in the registry and page title.
2. **App rename** — `ideations/inthub-inventory` → `ideations/inthub-v1`, package `@rp-vibe-ideation/inthub-inventory` → `@rp-vibe-ideation/inthub-v1`.
3. **Lib rename** — `packages/inthub-data-inventory` → `packages/inthub-data`, package `@rp-vibe-ideation/inthub-data-inventory` → `@rp-vibe-ideation/inthub-data`.

Run `pnpm install` after the renames to regenerate `pnpm-lock.yaml`.

---

## 1. UI changes (in `app-sidebar.tsx`)

Remove the `IntHubHeader` function and the `<SidebarHeader>` block entirely.
The sidebar now opens directly with `<SidebarContent>` (OrgSwitcher, then nav sections).

Before:
```tsx
<Sidebar collapsible="icon" {...props}>
  <SidebarHeader>
    <IntHubHeader />
  </SidebarHeader>
  <SidebarContent>
    ...
  </SidebarContent>
  ...
```

After:
```tsx
<Sidebar collapsible="icon" {...props}>
  <SidebarContent>
    ...
  </SidebarContent>
  ...
```

Also remove: the `IntHubHeader` function definition, and the `Network` lucide import (used only by `IntHubHeader`).

---

## 2. App rename: `inthub-inventory` → `inthub-v1`

### Folder
```bash
git mv ideations/inthub-inventory ideations/inthub-v1
```

### `ideations/inthub-v1/package.json`
```json
{ "name": "@rp-vibe-ideation/inthub-v1" }
```
Also update the `@rp-vibe-ideation/inthub-data-inventory` dependency key → `@rp-vibe-ideation/inthub-data`.

### `ideations/inthub-v1/project.json`
```json
{
  "name": "@rp-vibe-ideation/inthub-v1",
  "targets": {
    "copy": {
      "dependsOn": ["build"],
      "command": "node -e \"require('fs').cpSync('ideations/inthub-v1/dist','apps/web/public/apps/inthub-v1',{recursive:true,force:true})\"",
      "options": { "cwd": "{workspaceRoot}" }
    }
  }
}
```

### `ideations/inthub-v1/vite.config.mts`
- `base: '/apps/inthub-v1/'`
- `cacheDir: '../../node_modules/.vite/ideations/inthub-v1'`

### `ideations/inthub-v1/index.html`
```html
<title>IntHub v1</title>
```

### `ideations/inthub-v1/tsconfig.app.json`
Update the `references` entry for inthub-data-inventory:
```json
{ "path": "../../packages/inthub-data/tsconfig.lib.json" }
```

### `apps/web/project.json`
In both `build.dependsOn` and `dev.dependsOn`, replace:
- `"@rp-vibe-ideation/inthub-inventory:copy"` → `"@rp-vibe-ideation/inthub-v1:copy"`

### `packages/ideation-registry/src/lib/ideation-registry.ts`
```ts
{
  id: 'inthub-v1',
  name: 'IntHub v1',
  description: 'Read-only inventory view of the IntHub data model',
  group: 'IntHub',
  url: '/apps/inthub-v1',
  devUrl: 'http://localhost:4204',
}
```

---

## 3. Lib rename: `inthub-data-inventory` → `inthub-data`

### Folder
```bash
git mv packages/inthub-data-inventory packages/inthub-data
```

### `packages/inthub-data/package.json`
```json
{ "name": "@rp-vibe-ideation/inthub-data" }
```

### `packages/inthub-data/tsconfig.json`
Update `tsBuildInfoFile` path if it references the old name (check before editing).

### `packages/inthub-data/tsconfig.lib.json`
Update `tsBuildInfoFile` path if it references the old name.

### `packages/inthub-data/tsconfig.spec.json`
Update `tsBuildInfoFile` path if it references the old name.

### `packages/inthub-data/jest.config.ts`
Update `displayName` if it references the old name.

### All import sites — replace `@rp-vibe-ideation/inthub-data-inventory` → `@rp-vibe-ideation/inthub-data`

Files to update (grep for `inthub-data-inventory` first to confirm the full list):
- `ideations/inthub-v1/package.json` — dependency key (done in step 2 above)
- `ideations/inthub-v1/src/app/components/app-sidebar.tsx`
- `ideations/inthub-v1/src/app/pages/provider-accounts-page.tsx`
- `ideations/inthub-v1/src/app/pages/credentials-page.tsx`
- `ideations/inthub-v1/src/app/pages/integrations-page.tsx`
- `ideations/inthub-v1/src/app/pages/orgs-page.tsx` — deleted in p5-014; skip
- Any other page that imports from `inthub-data-inventory`

---

## 4. Post-rename

```bash
pnpm install          # regenerate pnpm-lock.yaml with new package names
pnpm nx reset         # clear Nx project graph cache
```

---

## Definition of Done

- `ideations/inthub-v1/` exists; `ideations/inthub-inventory/` does not
- `packages/inthub-data/` exists; `packages/inthub-data-inventory/` does not
- `grep -r "inthub-inventory" ideations/ packages/ apps/ packages/ideation-registry/` returns no matches (except git history)
- `grep -r "inthub-data-inventory" ideations/ packages/` returns no matches
- Sidebar has no IntHub branding header — OrgSwitcher is the first element
- Shell registry shows "IntHub v1" with url `/apps/inthub-v1`
- `pnpm nx test inthub-v1` passes
- `pnpm nx test inthub-data` passes
- `pnpm nx run @rp-vibe-ideation/inthub-v1:copy` succeeds — assets land in `apps/web/public/apps/inthub-v1/`
- `pnpm nx build web` exits 0
