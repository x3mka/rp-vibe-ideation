# p5-019 — MSSP org flag, provider config schema, account config, SHQ Response

**Status:** `done`

---

## Changes

### 1. Entities
- `org.ts`: add `isMssp?: boolean`
- `provider.ts`: add `Platform` to ProviderCategory; add `configSchema?: Record<string, unknown>`
- `provider-account.ts`: remove `region?: string`; add `config?: Record<string, unknown>`

### 2. Data
- `org-shq`: add `isMssp: true`
- AWS provider: add `configSchema: { default_region }`. QRadar provider: add `configSchema: { url }`
- Add `shq-response` provider (Platform category)
- Convert AWS account `region` → `config: { default_region }` for acme/titan/vertex
- Add `config: { url }` to `pa-titan-qradar`
- Add `pa-shq-qradar-1`, `pa-shq-qradar-2` (SHQ-owned QRadar consoles, with config.url)
- Add `pa-shq-asset-mgmt` (shq-response provider, SHQ owner)
- Add `it-aws-asset-mgmt-v1`: DataPipeline, aws→shq-response, Astronomer, description "AWS cloud assets discovery using AWS Resource Explorer"
- Add `int-acme-aws-assets`, `int-titan-aws-assets`, `int-vertex-aws-assets`

### 3. UI
- `app-sidebar.tsx` OrgSwitcher: pass `isMssp`; show "(MSSP)" in label; sort MSSP orgs first
- `provider-accounts-page.tsx`: remove Region column; add Config column (key: value pairs)
- `integrations-page.tsx`: Integration Type cell shows name + kind + runtime on second line

---

## Definition of Done
- `org-shq.isMssp === true`; SecurityHQ shows as "SecurityHQ (MSSP)" in switcher, listed first
- AWS account `region` field removed; `config.default_region` present
- `pa-shq-qradar-1/2` exist with `config.url`; QRadar provider has `configSchema`
- `pa-shq-asset-mgmt` exists; `shq-response` provider exists
- 3 AWS asset integrations exist
- Provider Accounts page has Config column, no Region column
- Integrations page Integration Type shows kind + runtime
- `pnpm nx test inthub-data` passes; `pnpm nx build web` exits 0
