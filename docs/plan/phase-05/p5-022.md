# p5-022 — Simplify Integration fields: accountId, credentialId, config

**Status:** `done`

---

## Changes

### 1. Entities
- `integration.ts`: rename `sourceAccountId?` → `accountId?`; rename `sourceCredentialId?` → `credentialId?`; remove `targetAccountId` and `targetCredentialId`; add `config?: Record<string, unknown>`
- `integration-type.ts`: add `configSchema?: Record<string, unknown>`

### 2. Data
- All integrations: rename fields; remove target account/credential fields

### 3. API
- `integrations.ts`: fix `getIntegrationGraph` to use `accountId` instead of `sourceAccountId`/`targetAccountId`

### 4. Tests
- `data.spec.ts`: update FK tests for renamed/removed fields
- `make-api.spec.ts`: update fixture integration objects

### 5. UI
- `integrations-page.tsx`: update `intg.sourceAccountId` → `intg.accountId`; remove target account lookup

---

## Definition of Done
- `Integration` has `accountId?`, `credentialId?`, `config?`; no target fields
- `IntegrationType` has `configSchema?`
- `pnpm nx test inthub-data` and `pnpm nx test inthub-api` pass
- `pnpm nx build web` exits 0
