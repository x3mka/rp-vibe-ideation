# p5-021 — Accounts column, cron descriptions, Sentinel detection rules provisioning

**Status:** `done`

---

## Changes

### 1. Data
- Add `cred-acme-sentinel` credential for `pa-acme-sentinel` (ct-sentinel-api)
- Add `it-sentinel-detection-rules-v1`: Provisioning, target sentinel, N8N runtime, weekly defaultSchedule
- Add `int-acme-sentinel-rules` integration for Acme Sentinel account

### 2. UI — integrations-page.tsx
- Add Accounts column: source account name → target account name (source omitted for provisioning)
- Schedule column: add human-readable cron description on second line

### 3. UI — integration-types-page.tsx
- Default Schedule column: add human-readable cron description on second line

---

## Definition of Done
- `it-sentinel-detection-rules-v1` exists; `int-acme-sentinel-rules` exists
- Integrations page has Accounts column showing source → target account names
- Both schedule columns show cron + human-readable description below
- `pnpm nx test inthub-data` passes; `pnpm nx build web` exits 0
