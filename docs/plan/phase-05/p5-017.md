# p5-017 — IntegrationType kind field, descriptive names, DataDog provisioning type

**Status:** `done`

---

## Changes

### 1. Entity — `packages/inthub-entities/src/lib/integration-type.ts`
- Add `IntegrationTypeKind` enum: `DataPipeline`, `Provisioning`
- Add `kind: IntegrationTypeKind` to `IntegrationType`
- Make `sourceProviderId` optional (not applicable for Provisioning types)
- Export `IntegrationTypeKind` from `index.ts`

### 2. Entity — `packages/inthub-entities/src/lib/integration.ts`
- Make `sourceAccountId` optional (not applicable for Provisioning integrations)

### 3. Data — `packages/inthub-data/src/lib/data.ts`
- Add `IntegrationTypeKind` to imports
- Update all 5 pipeline integration types: add `kind: DataPipeline`, rename with explicit data description, fix Sentinel runtime to Astronomer
  - `it-qualys-clickhouse-v1`: "Exposures Qualys → ClickHouse v1"
  - `it-qualys-clickhouse-v2`: "Exposures Qualys → ClickHouse v2"
  - `it-datadog-clickhouse-v1`: "Alerts DataDog → ClickHouse"
  - `it-tenable-clickhouse-v1`: "Exposures Tenable → ClickHouse"
  - `it-sentinel-clickhouse-v1`: "Alerts Sentinel → ClickHouse", runtimeId → `rt-astronomer-prod`
- Add new provisioning integration type: `it-datadog-detection-rules-v1`
  - kind: Provisioning, targetProviderId: datadog, runtime: Astronomer
- Add provisioning integration for Acme (has pa-acme-datadog): `int-acme-dd-rules`

### 4. UI — `ideations/inthub-v1`
- `integration-types-page.tsx`: drop Source→Target column, add Kind badge + Description columns
- `integrations-page.tsx`: drop Source→Target account column

---

## Definition of Done
- `IntegrationTypeKind` exported from `inthub-entities`
- All 5 pipeline types have `kind: DataPipeline` and descriptive names
- Sentinel type uses Astronomer runtime
- `it-datadog-detection-rules-v1` exists with `kind: Provisioning`
- `int-acme-dd-rules` exists in integrations
- Integration Types page shows Kind badge and Description, no Source→Target
- Integrations page has no Source→Target column
- `pnpm nx test inthub-data` passes
- `pnpm nx build web` exits 0
