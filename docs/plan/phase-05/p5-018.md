# p5-018 — ServiceNow provider, SHQ SIR/ITSM account, DataDog→SIR integration type

**Status:** `done`

---

## Changes

### 1. Data — `packages/inthub-data/src/lib/data.ts`

**Providers** — add ServiceNow:
- `id: 'servicenow'`, category: ITSM, name: 'ServiceNow'

**Credential Types** — add ServiceNow API:
- `ct-servicenow-api`: providerId: servicenow, fields: `['instanceUrl', 'username', 'apiKey']`

**Provider Accounts** — add SHQ ServiceNow account, rename SHQ ClickHouse:
- `pa-shq-servicenow`: orgId org-shq, providerId servicenow, name "SHQ SIR/ITSM", owner SHQ
- `pa-shq-clickhouse` name → "SHQ Data Warehouse"

**Credentials** — add SHQ ServiceNow credential:
- `cred-shq-servicenow` for `pa-shq-servicenow`, ct-servicenow-api

**Integration Types** — replace DataDog→ClickHouse with DataDog→ServiceNow SIR:
- Remove `it-datadog-clickhouse-v1`
- Add `it-datadog-sir-v1`: name "DataDog Alerts → SHQ SIR Incidents", kind DataPipeline,
  source datadog, target servicenow, runtime N8N, dagId `datadog_to_sir_v1`

**Integrations** — update `int-acme-dd-ch-1`:
- integrationTypeId → `it-datadog-sir-v1`
- targetAccountId → `pa-shq-servicenow`
- targetCredentialId → `cred-shq-servicenow`
- name → "Acme DataDog → SHQ SIR Incidents"

### 2. No UI changes needed
New provider, cred type, and account appear automatically in their respective pages.
The integration type rename shows in Integrations and Integration Types pages automatically.

---

## Definition of Done
- ServiceNow provider in providers list
- `pa-shq-servicenow` with owner SHQ exists
- `pa-shq-clickhouse` name is "SHQ Data Warehouse"
- `it-datadog-sir-v1` exists with N8N runtime; `it-datadog-clickhouse-v1` removed
- `int-acme-dd-ch-1` targets `pa-shq-servicenow`
- `pnpm nx test inthub-data` passes
- `pnpm nx build web` exits 0
