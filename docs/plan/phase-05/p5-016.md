# p5-016 — ProviderAccount owner field, SHQ ClickHouse account, admin email

**Status:** `done`

Introduce an `owner` field on `ProviderAccount` (Customer | SHQ), consolidate all ClickHouse accounts into one shared SHQ-owned account, and update the admin email in the sidebar.

---

## Changes

### 1. Entity — `packages/inthub-entities/src/lib/provider-account.ts`
- Add `ProviderAccountOwner` enum: `Customer = 'Customer'`, `SHQ = 'SHQ'`
- Add `owner: ProviderAccountOwner` field to `ProviderAccount` interface
- Export `ProviderAccountOwner` from `packages/inthub-entities/src/index.ts`

### 2. Data — `packages/inthub-data/src/lib/data.ts`
- Add `org-shq` org: SecurityHQ (createdAt 2020-01-01)
- Add `ProviderAccountOwner` to imports
- Add `owner: ProviderAccountOwner.Customer` to all existing provider accounts
- Remove `pa-acme-clickhouse`, `pa-titan-clickhouse`, `pa-vertex-clickhouse`
- Add `pa-shq-clickhouse`: orgId `org-shq`, providerId `clickhouse`, owner `SHQ`
- Remove `cred-acme-clickhouse`, `cred-titan-clickhouse`, `cred-vertex-clickhouse`
- Add `cred-shq-clickhouse` for `pa-shq-clickhouse`
- Remove `cfg-acme-clickhouse`, `cfg-titan-clickhouse`, `cfg-vertex-clickhouse`
- Add `cfg-shq-clickhouse` for `pa-shq-clickhouse`
- Remove `prov-titan-clickhouse-table`, `prov-vertex-clickhouse-table`
- Add `prov-shq-clickhouse-table` for `pa-shq-clickhouse`
- Update all 5 integrations: `targetAccountId → 'pa-shq-clickhouse'`, `targetCredentialId → 'cred-shq-clickhouse'`

### 3. UI — `ideations/inthub-v1`
- `provider-accounts-page.tsx`: add Owner badge column (SHQ = `default` variant, Customer = `secondary`)
- `app-sidebar.tsx`: `admin@inthub.io` → `admin@securityhq.com`

---

## Definition of Done
- `ProviderAccountOwner` exported from `inthub-entities`
- `pa-shq-clickhouse` exists; `pa-acme-clickhouse`, `pa-titan-clickhouse`, `pa-vertex-clickhouse` do not
- All integrations target `pa-shq-clickhouse` / `cred-shq-clickhouse`
- Owner badge visible in Provider Accounts page
- Admin email is `admin@securityhq.com`
- `pnpm nx test inthub-data` passes
- `pnpm nx build web` exits 0
