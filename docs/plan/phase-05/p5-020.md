# p5-020 — Schedule fields, rename SHQ Response Assets, remove integration name, fix badge

**Status:** `done`

---

## Changes

### 1. Entities
- `integration-type.ts`: add `defaultSchedule?: string`
- `integration.ts`: make `name?: string` (optional); add `schedule?: string`

### 2. Data
- `pa-shq-asset-mgmt`: rename "SHQ Asset Management" → "SHQ Response Assets"
- Remove `pa-shq-qradar-1` and `pa-shq-qradar-2`
- Integration type names — replace "ClickHouse" with "SHQ Data Warehouse":
  - `it-qualys-clickhouse-v1`: "Exposures Qualys → SHQ Data Warehouse v1"
  - `it-qualys-clickhouse-v2`: "Exposures Qualys → SHQ Data Warehouse v2"
  - `it-tenable-clickhouse-v1`: "Exposures Tenable → SHQ Data Warehouse"
  - `it-sentinel-clickhouse-v1`: "Alerts Sentinel → SHQ Data Warehouse"
  - `it-aws-asset-mgmt-v1`: "AWS Assets → SHQ Response Assets"
- Add `defaultSchedule` cron strings to all active integration types
- Remove `name` field from all integrations
- Add `schedule` overrides to a few integrations

### 3. UI
- `integration-types-page.tsx`: fix `kindVariant()` — Provisioning → `'default'`, DataPipeline → `'outline'`; add Default Schedule column
- `integrations-page.tsx`: remove Name column; add Schedule column (own schedule, or type's defaultSchedule with muted styling)

---

## Definition of Done
- `pa-shq-asset-mgmt` name is "SHQ Response Assets"; `it-aws-asset-mgmt-v1` name matches
- No integration type name contains "ClickHouse"
- `pa-shq-qradar-1/2` do not exist in data
- Provisioning badge uses `'default'` variant; DataPipeline uses `'outline'`
- Integration types have `defaultSchedule`; some integrations have `schedule` override
- Integrations page has no Name column; has Schedule column
- `pnpm nx test inthub-data` passes; `pnpm nx build web` exits 0
